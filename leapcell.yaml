build:
  buildCommand: pip install -r requirements.txt

run:
  runCommand: uvicorn app:app --host 0.0.0.0 --port 8080

envs:
  - name: DATABASE_URL
  - name: TEAMS_TABLE
  - name: STUDENTS_TABLE
  - name: RESULTS_TABLE
  - name: ADMIN_PASSWORD
